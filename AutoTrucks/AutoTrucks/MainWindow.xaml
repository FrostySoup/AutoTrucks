<Window x:Class="AutoTrucks.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AutoTrucks"
        mc:Ignorable="d"
        Title="MainWindow">
    
    <Grid x:Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="6*"/>
        </Grid.RowDefinitions>
        <ContentControl Grid.Row="0" Content="{Binding Path = TopButtonsViewModel}" />
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <ContentControl Grid.Column="0" Content="{Binding Path = PostLoadsViewModel}"/>
            <ContentControl Grid.Column="1" Content="{Binding Path = SearchLoadsViewModel}"/>
            <ContentControl Grid.Column="2" Content="{Binding Path = SearchTrucksViewModel}"/>
            <ContentControl Grid.Column="3" Content="{Binding Path = PostTrucksViewModel}"/>
        </Grid>
    </Grid>
    
</Window>

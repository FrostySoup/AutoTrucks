<UserControl x:Class="View.MainWindowViews.TopButtonsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:View.MainWindowViews"
             mc:Ignorable="d" 
             d:DesignHeight="70" d:DesignWidth="400">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <StackPanel Orientation="Horizontal">
            <Button Style="{StaticResource MainWindowTopButtons}">Post Loads</Button>
            <Button Style="{StaticResource MainWindowTopButtons}">Post Trucks</Button>
            <Button Style="{StaticResource MainWindowTopButtons}">Search Loads</Button>
            <Button Style="{StaticResource MainWindowTopButtons}">Search Trucks</Button>
        </StackPanel>
    </Grid>
    
</UserControl>
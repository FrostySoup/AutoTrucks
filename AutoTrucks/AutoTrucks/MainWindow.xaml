<Controls:MetroWindow x:Class="AutoTrucks.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:local="clr-namespace:AutoTrucks"       
        mc:Ignorable="d"
        Height="800"
        Width="1200"
        Title="MainWindow">
    
    <Grid x:Name="MainGrid">

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="6*"/>
        </Grid.RowDefinitions>
        <ContentControl Grid.Row="0" Content="{Binding Path = TopButtonsViewModel}" />
            <UniformGrid Grid.Row="1" Rows="1">
            <ContentControl Grid.Row="0" Content="{Binding Path = PostLoadsViewModel}"/>
            <ContentControl Grid.Row="0" Content="{Binding Path = SearchLoadsViewModel}"/>
            <ContentControl Grid.Row="0" Content="{Binding Path = SearchTrucksViewModel}"/>
            <ContentControl Grid.Row="0" Content="{Binding Path = PostTrucksViewModel}"/>
        </UniformGrid>
    </Grid>
    
</Controls:MetroWindow>
